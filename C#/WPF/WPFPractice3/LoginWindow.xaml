<Window x:Class="WPFPractice3.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFPractice3"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:LoginViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Username:" Grid.Row="0"/>
        <TextBox Text="{Binding UserId}" Grid.Row="0" Margin="80,0,0,0"/>

        <Label Content="Password:" Grid.Row="1" Margin="0,10,0,0"/>
        <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" 
                     Grid.Row="1" 
                     Margin="80,10,0,0" 
                     KeyDown="PasswordBox_KeyDown"/>

        <Button Content="Login" Grid.Row="2" Margin="0,20,0,0" Command="{Binding LoginCommand}"/>

        <TextBlock Text="{Binding ErrorMessage}" Grid.Row="3" Foreground="Red" Margin="0,10,0,0"/>
    </Grid>
</Window>
